# JOIN Operation
# INNER JOIN ---  only the common from both must be displayed 

# select column from table1 inner join table2 on table1.columnname = table2.columnname

# LEFT JOIN ------------> ALL left table element must be counted if not possible than add null value for those element which are not available in right table
# Same rught join

